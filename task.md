
You are a senior software engineer.
Create a Symfony Console wrapper that executes existing Bash scripts as commands—**do not rewrite the Bash logic**.

### Project structure (must match exactly):
```
.
├── composer.json
├── scripts/
│    ├── sysinfo.sh          # print information of system.  
│    └── laravel/
│        ├── setup.sh        # initialize the laravel project.
│        ├── clean.sh        # remove all the temporary and sensitive data.
│        └── service.sh      # start/stop/status/restart services.    
├── src/
│   └── func.php
├── tests/
└── bin/
    └── xbot  # main executable
```

### Requirements:
1. **`bin/xbot`** is a PHP script using `symfony/console`.
2. It exposes three commands:
   - `xbot clean` → runs `./scripts/laravel/clean.sh`
   - `xbot setup` → runs `./scripts/laravel/setup.sh`
   - `xbot sysinfo` → runs `./scripts/sysinfo.sh`
3. Each command:
   - Shows a colored status message before/after execution
   - Passes all arguments/options through to the underlying script
   - Uses `proc_open()` or `passthru()` to preserve real-time output
4. Add `--help` auto-generated by Symfony Console.
5. Include minimal `composer.json` requiring only `symfony/console`.

### Deliverables:
- Only output the **full content of these files**:
  - `composer.json`
  - `bin/xbot`
  - `src/func.php` (can be empty or contain helper utils)
- Assume `laravel/*.sh` and `sysinfo.sh` already exist and are executable.
- Do **not** modify or duplicate Bash scripts.

### Rules:
- No explanations, no markdown, no extra files.
- Output raw file contents with clear separators (e.g., `### FILE: path/to/file`).
- Keep PHP code minimal and secure (validate paths, avoid `shell_exec` if possible).

--- 

This prompt ensures your agent generates **only the PHP wrapper**, reuses your existing Bash scripts as-is, and saves tokens by being direct and English-only. 💸